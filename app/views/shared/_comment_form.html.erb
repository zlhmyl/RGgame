<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "填写想要评论的内容..." %>
  </div>
  <%= f.submit "回复", class: "btn btn-primary" %>
<% end %>